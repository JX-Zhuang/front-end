//合作院校  合作企业
/*$(window).ready(function() {
    var inner = $('.school-inner'),
        prev = $('.school-left-btn'),
        next = $('.schoolright-btn'),
        move = false;
    //左按键
    prev.click(function() {
        if (!move) {
            move = true;
            inner.animate({
                'left': parseInt(inner.css('left')) - 140
            }, 500, function() {
                if (parseInt(inner.css('left')) < -1260) {
                    inner.css('left', 0);
                }
                move = false;
            });
        }
    });
    //右按键
    next.click(function() {
        if (!move) {
            move = true;
            inner.animate({
                'left': parseInt(inner.css('left')) + 140
            }, 500, function() {
                if (parseInt(inner.css('left')) > -140) {
                    inner.css('left', -1400);
                }
                move = false;
            });
        }
    });

});*/
